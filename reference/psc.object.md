# Fitted `psc` object

An object returned by the `pscfit` function, inheriting from class `psc`
and representing a fitted personalised synthetic control model.

## Usage

``` r
psc.object
```

## Format

An object of class `NULL` of length 0.

## Author

Richard Jasckson (<richj23@liverpool.ac.uk>)
